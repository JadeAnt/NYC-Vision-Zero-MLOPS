# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: NYC-Vision-Zero-app
  namespace: NYC-Vision-Zero-production
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: NYC-Vision-Zero-app
  template:
    metadata:
      labels:
        app: NYC-Vision-Zero-app
    spec:
      containers:
        - name: NYC-Vision-Zero-app
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: 8000
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
# Service
apiVersion: v1
kind: Service
metadata:
  name: NYC-Vision-Zero-app
  namespace: NYC-Vision-Zero-production
spec:
  selector:
    app: NYC-Vision-Zero-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  externalIPs:
    - {{ .Values.service.externalIP }}
